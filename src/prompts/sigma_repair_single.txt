The Sigma rule below failed deterministic pySigma or Huntable policy validation. Fix every listed error while preserving the detection logic, then output the corrected rule.

VALIDATION ERRORS
{validation_errors}

PREVIOUS ATTEMPT
{original_rule}

HOW TO READ THE ERRORS
- "mapping values are not allowed here" or "could not find expected ':'" -> prose, a code fence, or unquoted special characters leaked into the YAML. Remove everything that is not the rule and quote the offending value.
- "Detection must contain a 'condition' key" -> add `condition: selection` (or the correct selection name) under `detection`.
- "Invalid search identifier" / "dangling" -> the condition references a selection that does not exist; make the names match.
- "Missing required field" -> add the field with a real value per the standard below.
- "references revoked ATT&CK technique ... use 'attack.tXXXX'" -> replace the tag with the named replacement.
- "Invalid level" / "Unknown status" -> use only the allowed vocabularies below.
- pySigma SigmaModifierError / SigmaValueError -> the modifier or value form is invalid; `*value*` becomes `|contains`, `|all` needs a list, regex must be valid.

HUNTABLE RULE STANDARD (must hold after the repair)
- logsource: `category` + `product` (windows | linux | macos), or `service` + an `EventID` in the selection. Never `service: sysmon`; map Sysmon events to their category and drop the EventID.
- Field names from the SigmaHQ taxonomy only (Image, CommandLine, ParentImage, OriginalFileName, TargetObject, Details, TargetFilename, ImageLoaded, DestinationIp, DestinationPort, DestinationHostname, Initiated, QueryName, ScriptBlockText, c-uri, c-useragent, cs-method). Never url, UserAgent, HttpMethod, EventCode, or SIEM-specific names.
- detection: selection mappings plus a `condition` string; single-quoted string values; Windows paths with single backslashes; `|contains|all` for multi-token command lines; no `*value*` wildcards.
- tags: `attack.<tactic>` AND `attack.t<id>[.<sub>]` per behavior; tactic names lowercase and hyphenated (defense-evasion, command-and-control, credential-access, privilege-escalation, lateral-movement).
- title: Title Case, no "Detects" prefix, at most 120 characters. description: starts with "Detects". status: experimental. level: informational | low | medium | high | critical (default medium). falsepositives: non-empty list with a concrete scenario. date: YYYY-MM-DD. id: UUIDv4 (keep the existing one if valid).
- Keep author, date, references, and any other valid field already present. Remove non-Sigma keys (observables_used, platform, telemetry_category, logsource_hint) and any other custom key.
- Do not broaden, narrow, or "improve" the detection beyond what the errors require.

OUTPUT FORMAT
- Output ONLY the corrected rule as YAML. Start immediately with `title:`. No explanations, markdown, or code fences.
- 2-space indentation, lowercase keys. Wrap title or description in double quotes if the value contains any of `? : [ ] {{ }} | & * # @ \``.
- Field order: title, id, status, description, references, author, date, tags, logsource, detection, falsepositives, level.

EXAMPLE OF CORRECT OUTPUT
title: Encoded PowerShell Command Execution
id: 9b2f4d6e-3a1c-4e8f-b7d0-2c5a8e1f4b6d
status: experimental
description: Detects PowerShell launched with an encoded command argument, a pattern commonly used to hide script content from command-line logging.
references:
  - https://example.com/report
author: Huntable CTI Studio
date: 2026-09-03
tags:
  - attack.execution
  - attack.t1059.001
  - attack.defense-evasion
  - attack.t1027.010
logsource:
  category: process_creation
  product: windows
detection:
  selection:
    Image|endswith:
      - '\powershell.exe'
      - '\pwsh.exe'
    CommandLine|contains|windash: ' -enc'
  condition: selection
falsepositives:
  - Administrative scripts distributed by management tooling that use encoded commands
level: medium

EXAMPLE OF INCORRECT OUTPUT (never do this)
Okay, let's tackle this. Looking at the errors, the first issue is...
```yaml
title: Encoded PowerShell Command Execution
```

Now output ONLY the corrected YAML starting with `title:`.
