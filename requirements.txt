# CTI Scraper Dependencies
# Pinned versions for reproducible builds
# Last updated: 2025-01-29
# Security audit: Updated all packages to latest versions
# Phase 1 security updates: 2025-01-29 (certifi, charset-normalizer, cryptography)
# Phase 2 security updates: 2025-01-07 (urllib3, aiohttp, starlette)
# Phase 3 security updates: 2026-04-04 (urllib3 2.6.3, langfuse 4.0.6)
# Phase 4 security updates: 2026-04-19 (filelock 3.29.0, requests 2.33.1, pygments 2.20.0)
# Phase 5 security updates: 2026-04-19 (cryptography 46.0.7, transitive dep floor pins)
# Phase 6 security updates: 2026-05-12 (urllib3 2.7.0, python-multipart 0.0.27, GitPython 3.1.50, langchain-core 1.3.3, Mako 1.3.12)

# Core scraping and processing
requests==2.33.1
httpx==0.28.1
brotli==1.2.0  # Brotli decompression support for httpx
beautifulsoup4==4.14.3
lxml==6.1.1
feedparser==6.0.12
extruct==0.18.0
iocextract==1.16.1
trafilatura==2.0.0  # Content extraction fallback for selector inference in source healing
playwright==1.58.0  # Headless browser for JS-rendered content

# Data processing and storage
pandas==3.0.2
numpy==2.4.3
sqlalchemy[asyncio]==2.0.49
asyncpg==0.31.0
PyYAML==6.0.3
psycopg2-binary==2.9.11
alembic==1.18.4

# Web framework and server
fastapi==0.136.3
starlette==1.0.1
uvicorn[standard]==0.41.0
jinja2==3.1.6

# Background task processing
celery==5.6.2
redis==7.4.0

# Classification and ML
scikit-learn==1.8.0
lightgbm==4.6.0
joblib==1.5.3
spacy==3.8.14  # Sentence splitting (sentencizer component only, ~2MB)

# Utilities
click==8.3.3
rich==14.3.3
pydantic==2.12.5
pydantic-settings==2.13.1

reportlab==4.4.10  # PDF generation
python-dateutil==2.9.0.post0  # Date parsing utilities

# Security
certifi==2026.5.20
urllib3==2.7.0
filelock==3.29.0  # CVE-2026-22701 fix
pygments==2.20.0  # CVE-2026-4539 fix
charset-normalizer==3.4.7
python-multipart==0.0.27
cryptography==48.0.0  # CVE-2026-34073, CVE-2026-39892 fixes

# PDF processing
pypdf==6.10.2

# SIGMA rule validation and repository management
pysigma==0.11.23
GitPython==3.1.50

# Additional AI/ML dependencies for enhanced features
torch==2.11.0  # For advanced ML models
transformers==5.8.0  # For LLM integration
accelerate==1.13.0  # For model optimization
openai==2.38.0  # Official OpenAI client
tiktoken==0.13.0  # Token counting utilities
sentence-transformers==5.5.1  # For semantic embeddings
pgvector==0.4.2  # PostgreSQL vector extension support

# Agentic workflow
langgraph==1.1.8  # LangGraph for agentic workflow orchestration
langgraph-checkpoint==4.1.1  # LangGraph checkpointing for state persistence
langgraph-checkpoint-postgres==3.0.5  # PostgreSQL checkpoint backend for LangGraph
langgraph-cli==0.4.26  # LangGraph CLI for HTTP server
langfuse==4.5.0  # Langfuse for LLM observability and tracing

# MCP server
mcp[cli]==1.27.1  # Model Context Protocol SDK for LLM tool integration

# Security pins (transitive dependency floors)
Authlib==1.7.2  # CVE fix: JWS header injection, Bleichenbacher oracle, OIDC hash bypass
Pillow==12.2.0  # CVE-2026-40192: FITS GZIP decompression bomb
langchain-core==1.3.3  # CVE-2026-40087 + CVE-2026-44843 fixes
langsmith==0.8.3  # streaming token events bypass output redaction
Mako==1.3.12  # path traversal + CVE-2026-44307 fixes
python-multipart==0.0.27  # CVE-2026-40347: DoS via large multipart preamble/epilogue
idna==3.16  # CVE-2026-45409 fix; exact pin, mirrors pyproject.toml
pymdown-extensions==10.21.3  # CVE-2026-46338 fix

# Documentation
mkdocs==1.6.1  # Static docs site generator
mkdocs-material==9.7.6  # MkDocs theme used for published docs
