# Auto-detect Docker environment and skip non-Docker tests
# Place this in project root for automatic pytest configuration

[conftest]
# Skip tests marked requires_docker if not in Docker
docker_auto_skip = true
