bert_base:
  model_name: bert-base-uncased
  batch_size: 8
  learning_rate: 5.0e-5
  epochs: 3

secbert:
  model_name: ${SECBERT_MODEL_NAME:-bert-base-uncased}
  batch_size: 8
  learning_rate: 3.0e-5
  epochs: 3

roberta_base:
  model_name: roberta-base
  batch_size: 8
  learning_rate: 5.0e-5
  epochs: 3
