# Private workflow presets

Place workflow config preset JSON files here. They are **ignored by git** and will not be committed.

Use **Workflow** → **Import from file** and select a file from this directory to load a private preset.
