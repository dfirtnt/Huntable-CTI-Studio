# CTI Scraper Environment Configuration Template
# Copy this file to .env and update with your actual values
# cp .env.example .env

# =============================================================================
# REQUIRED: PostgreSQL Configuration
# =============================================================================
# Strong password required - change this!
POSTGRES_PASSWORD=your_secure_postgres_password_change_this

# Database connection (set automatically in docker-compose.yml)
# DATABASE_URL is constructed from POSTGRES_PASSWORD in docker-compose.yml
# Format: postgresql+asyncpg://cti_user:${POSTGRES_PASSWORD}@postgres:5432/cti_scraper

# =============================================================================
# OPTIONAL: LLM API Keys (for AI features)
# =============================================================================
# At least one LLM provider is recommended for full functionality
OPENAI_API_KEY=your_openai_api_key_here
ANTHROPIC_API_KEY=your_anthropic_api_key_here
CHATGPT_API_KEY=your_chatgpt_api_key_here

# =============================================================================
# OPTIONAL: LM Studio Configuration (for local LLM)
# =============================================================================
# Default: http://host.docker.internal:1234/v1
# Change if your LM Studio server is on a different host/port
LMSTUDIO_API_URL=http://host.docker.internal:1234/v1

# Model names (defaults set in docker-compose.yml, override here if needed)
LMSTUDIO_MODEL=deepseek/deepseek-r1-0528-qwen3-8b
LMSTUDIO_MODEL_RANK=qwen/qwen3-4b-2507
LMSTUDIO_MODEL_EXTRACT=qwen/qwen3-4b-2507
LMSTUDIO_MODEL_SIGMA=qwen/qwen3-4b-2507

# Embedding configuration (required for Sigma similarity search)
LMSTUDIO_EMBEDDING_URL=http://host.docker.internal:1234/v1/embeddings
LMSTUDIO_EMBEDDING_MODEL=text-embedding-e5-base-v2

# Context length overrides (if needed)
# LMSTUDIO_CONTEXT_LENGTH_gemma_3n_e4b_it_text=16384
# LMSTUDIO_CONTEXT_LENGTH_deepseek_deepseek_r1_0528_qwen3_8b=16384
# LMSTUDIO_CONTEXT_LENGTH_qwen_qwen3_4b_2507=16384

# =============================================================================
# OPTIONAL: LangSmith Tracing
# =============================================================================
# For LLM observability and tracing
LANGSMITH_API_KEY=

# =============================================================================
# OPTIONAL: Source Configuration
# =============================================================================
# Disable automatic YAML seeding on startup (default: false)
# Set to true to prevent auto-sync from config/sources.yaml
DISABLE_SOURCE_AUTO_SYNC=false

# =============================================================================
# NOTES
# =============================================================================
# 1. POSTGRES_PASSWORD is REQUIRED - set a strong password
# 2. LLM API keys are optional but recommended for AI features
# 3. LM Studio settings have defaults in docker-compose.yml
# 4. DATABASE_URL and REDIS_URL are set automatically in docker-compose.yml
# 5. For test environments, use env.test.template instead
