# Requirements for Extract Agent fine-tuning
# Install with: pip install -r requirements-finetune.txt

# Core fine-tuning libraries
transformers>=4.35.0
datasets>=2.14.0
peft>=0.6.0
accelerate>=0.24.0

# Quantization (for QLoRA)
bitsandbytes>=0.41.0

# PyTorch (install separately based on your CUDA version)
# For CUDA 11.8: pip install torch --index-url https://download.pytorch.org/whl/cu118
# For CPU only: pip install torch

# Optional but recommended
wandb>=0.15.0  # For experiment tracking
tensorboard>=2.14.0  # Alternative to wandb

