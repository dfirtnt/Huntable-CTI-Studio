---
name: lgl
description: Lite commit and push to current branch (git add ., commit, push)
disable-model-invocation: true
---

git add ., commit, then push the current branch (e.g. git push origin HEAD or git push).
