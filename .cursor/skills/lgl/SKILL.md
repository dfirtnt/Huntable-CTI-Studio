---
name: lgl
description: Lite commit and push to current branch (git add ., commit, push)
disable-model-invocation: true
---

git add ., commit, then push the **active/current branch** (e.g. `git push origin HEAD` or `git push`). Do not push to `origin main` unless the user explicitly asks to push to main.
